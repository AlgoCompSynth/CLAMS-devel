# Building the PlatformIO-Cforth container

1. On the host, run `./1_host_recreate_container`.
This rebuilds the container.

2. On the host, type `distrobox enter PlatformIO-Cforth`.
Then type `./2_dbx_install_all.sh`. This will install all
the software.
